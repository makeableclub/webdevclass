Javascript Arrays

var students = ["emily", "addision", "yiying", "mark"];
length, index (0-3, 4)
add: students[4]

initialize empty array:
var birds = [];
var birds = new Array();

var anything = [1, -3, "me", true, null];

array of array

string and array
split, join

Array methods:
push/pop, shift/unshift, indexOf, slice, splice

Array iteration:
for(int i=0; i<arr.length; i++) {}
arr.forEach( function(element) {});

//
// for an array:
1. create;
2. populate elements;
3. access any one of the elements;
4. add first element(unshift), or last element(push)
5. remove first element(shift), or last element(pop)
6. get a range of elements(slice), or remove a range of elements(splice)
